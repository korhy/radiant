<div class="mt-10">
    <p class="text-xs font-bold uppercase tracking-widest text-content-min mb-3">Mes apps</p>
    <div class="grid grid-cols-4 gap-2">
        {% for appEntry in apps %}
            <a href="{{ path(appEntry.route) }}"
               rel="noreferrer noopener"
               class="group flex flex-col items-center justify-center gap-2 rounded-xl border border-line/50 bg-surface-raised/60 text-content-low shadow-inner transition-all duration-200 hover:border-brand-fg/40 hover:bg-surface-raised hover:text-brand-fg active:scale-95"
               style="aspect-ratio:1/1;">
                <span class="h-6 w-6 transition-transform duration-200 group-hover:scale-110" aria-hidden="true">
                    {{ include('components/_icon_' ~ appEntry.slug ~ '.html.twig') }}
                </span>
                <span class="text-[10px] font-semibold uppercase tracking-wider leading-none">{{ appEntry.label }}</span>
            </a>
        {% endfor %}
    </div>
</div>
