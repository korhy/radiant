<section id="projects" class="mb-16 scroll-mt-16 md:mb-24 lg:mb-36 lg:scroll-mt-24">
    <twig:SectionHeader title="Projets" />
    <ol>
        {% for project in projects %}
            <li class="mb-12">
                <div class="lg:flex lg:justify-between lg:gap-1">
                    <header class="sm:col-span-2 lg:w-1/5 lg:flex-col">
                        <div class="basis-1/6"><img class="rounded-2xl" src="{{ asset('images/personal_projects/') }}{{ project.fileName }}" alt="">
                        </div>
                    </header>
                    <div class="sm:col-span-6 lg:w-4/5 lg:flex-col">
                        <h3 class="font-medium leading-snug text-content-high hover:text-brand-fg hover:underline">
                            <a href="{{ project.url }}" target="_blank" rel="noopener noreferrer">{{ project.name }}
                                <span class="sr-only">(nouvelle fenêtre)</span>
                                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewbox="0 2 24 24" stroke-width="1.5" stroke="currentColor" class="inline-block h-4 w-4">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 0 0 3 8.25v10.5A2.25 2.25 0 0 0 5.25 21h10.5A2.25 2.25 0 0 0 18 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25"/>
                                </svg>
                            </a>
                        </h3>
                        <p class="mt-2 text-sm leading-normal">
                            {{ project.description|raw }}
                        </p>
                        <ul class="mt-2 flex flex-wrap">
                            {% for tag in project.tags.tags %}
                                <li class="mr-1.5 mt-2">
                                    <twig:Badge variant="outline" class="border-2 border-brand-light px-3 py-1 leading-5 text-brand-fg">{{ tag }}</twig:Badge>
                                </li>
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </li>
        {% endfor %}
    </ol>
</section>
