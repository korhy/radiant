<nav class="flex items-center justify-between">
    <a href="{{ path('cookbook') }}" class="flex items-center gap-2">
        <span class="w-6 h-6 text-amber-400">{{ include('components/_icon_cookbook.html.twig') }}</span>
        <span class="text-white font-bold tracking-wide">Mon livre de recettes</span>
    </a>
</nav>