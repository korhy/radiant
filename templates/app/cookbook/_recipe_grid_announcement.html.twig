{# The sentence read out when a page is appended. Rendered here so no visible
   text lives in the controller or in the Stimulus controller.

   Variables: count (required). #}
{{ count }} recette{{ count > 1 ? 's' }} ajoutée{{ count > 1 ? 's' }} à la liste.
