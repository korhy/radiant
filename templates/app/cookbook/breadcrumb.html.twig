<nav aria-label="breadcrumb">
    <ol class="flex items-center gap-2 text-xs text-content-min">
        {% for crumb in breadcrumbs %}
            {% if not loop.last %}
                <li><a href="{{ crumb.path }}" class="hover:text-content-mid transition-colors">{{ crumb.label }}</a></li>
                <li aria-hidden="true">/</li>
            {% else %}
                <li class="text-content-mid truncate">{{ crumb.label }}</li>
            {% endif %}
        {% endfor %}
    </ol>
</nav>
