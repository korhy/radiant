{# @block content The revealed content. #}
<div class="{{ ('absolute z-10 ' ~ attributes.render('class'))|tailwind_merge }}" {{ attributes.defaults({'data-slot': 'info-disclosure-panel'}) }}>
    {%- block content %}{% endblock -%}
</div>
