/*
 * JS entry point, compiled by Webpack Encore and injected by
 * `encore_entry_script_tags('app')` in base.html.twig.
 *
 * Stimulus controllers are registered by hand in bootstrap.js: a controller
 * missing from that file never runs.
 */
import './bootstrap.js';
import './styles/app.css';
